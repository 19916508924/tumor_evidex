# Evidex · AI 开发约定

适用于整个仓库。每次开始任务、恢复上下文或准备修改代码时，先读取本文件、`README.md` 和 `docs/testing.md`，再检查目标目录是否有更具体的指令。不要依赖上次对话的记忆。

## 必须执行：测试先行

1. **明确验收**：先列出输入、可观察输出、正常路径、边界条件、失败路径和受影响的旧行为。
2. **先写测试**：新增行为先新增/修改可执行测试；修复 Bug 先补能复现问题的回归测试，再修改实现。
3. **RED**：运行相关测试，确认因预期行为尚未实现而失败。依赖缺失、语法错误或配置错误不算行为的 RED。
4. **GREEN**：做使测试通过的最小实现，不顺手改无关业务。
5. **REFACTOR**：在测试保护下整理代码，再运行受影响测试。每轮有意义的代码修改后都要验证，不必每次键入字符就运行全套测试。
6. **完整验收**：交付前运行 `pnpm verify`；页面、路由、鉴权、依赖或构建配置变化还必须运行 `pnpm verify:full`。失败先修复，再汇报完成。
7. **提供证据**：最终答复写明改动、RED/GREEN 的命令及实际结果、回归检查、尚未验证的范围。没有运行不能声称通过；基础设施阻塞不能标记为验收完成。

纯文档/注释/格式调整无需人为制造失败测试，但须先确认行为不变并运行相关检查。无行为变化的重构先运行现有测试并补足保护；新增测试架构的首批用例是现状基线，不把它们宣称为已完成业务 RED。这些例外不是跳过验证的理由。

## 测试与质量底线

- 运行环境：Node 24（`.nvmrc`）、`package.json` 指定的 pnpm；冻结锁文件安装，不混用 npm/yarn，不随意升级生产依赖。
- 测试分层：Vitest 单元测试 → Testing Library 组件测试 → Vitest 接口/服务集成测试 → Playwright 浏览器测试。异步 Server Component 用集成/E2E 验证。
- 新增或修改业务模块时，把能单测的模块纳入 `vitest.config.ts` 覆盖范围。初始范围不是全仓覆盖率。每个受保护文件：语句/行/函数 ≥90%，分支 ≥80%。无法单测的部分用集成/E2E 并说明。
- 按用户可观察行为断言，不复制实现。涉及鉴权、支付、额度、回调、持久化时补未授权、跨用户、非法输入、重复请求/幂等、失败回滚等适用场景。
- 单元/组件/接口测试默认拦截未声明的 HTTP 请求；只 mock 外部边界，不 mock 被测业务本身。数据库测试使用独立测试库/事务清理，禁止生产数据和真实付费 API。
- 不得为变绿删除断言、跳过测试、降低覆盖率门槛、关闭类型/静态检查、扩大遗留问题基线或伪造结果。不得提交 `.only`、`.skip`、`.todo`。
- 不擅自改环境变量含义、接口格式、数据库结构或生产行为。保留用户未提交的改动；迁移需有验证和恢复方案。
- 保持 README、本文件、测试指南和实际命令一致；新规则放在持久文件中，不只留在聊天里。

## Code Review Rules

- 新需求是否先有验收用例？Bug 是否有可复现的回归测试？断言是否真的能捕获问题？
- 代码变更是否附带相关测试？是否遗漏异常、权限或重要边界？
- CI 绿色不等于覆盖完整，也不能证明测试先于实现；审查 RED/GREEN 证据和测试内容。
- 发布前确认完整检查通过；不要用构建成功代替测试通过。
