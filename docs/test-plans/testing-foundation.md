# 自动化测试基础 · 验收记录

日期：2026-09-08。运行环境：Node 24.19.0、pnpm 10.30.3、macOS、本机 Google Chrome（Playwright 创建隔离会话）。

## 本次验收目标

- README 和 AGENTS.md 持久保存测试先行流程，并提供需求测试模板。
- 建立单元、组件、接口集成和真实服务 E2E 四层测试。
- 类型检查包含 Next.js 生成的路由约束；静态诊断不能新增，不能通过跳过用例规避测试。
- 业务源码变更附带测试变更，受保护模块逐文件执行覆盖率门槛。
- CI 完整检查通过后才进入原有 Docker 发布流程。

## RED → GREEN 证据

| 检查                   | 修改实现前的 RED                                                                                                       | 修复后的 GREEN                                                     |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| 源码变更必须附带测试   | Node assert 验证 `evaluateTestPolicy([{status: 'M', path: 'src/app/page.tsx'}]).ok === false`，初始返回 true，断言失败 | 最小实现后同一断言通过，5 个测试策略用例通过                       |
| 遗留静态诊断识别       | `vitest run tests/unit/lint-baseline.test.ts`：1 失败、3 通过                                                          | 实现文件哈希与诊断指纹同时匹配后，4 通过                           |
| 路由类型约束不能被排除 | `vitest run tests/unit/typecheck-config.test.ts`：生成的 validator 不在 TypeScript 文件列表，1 失败                    | 将 `.next` 整体排除改为仅排除 standalone，1 通过；实际类型检查通过 |

限流、响应协议、按钮、用户信息接口等首批业务测试是现有模板的行为基线，没有改写对应业务实现，不把这些现状用例宣称为业务功能的 RED/GREEN。

## 最终完整验收

实际执行：

```bash
CI=true QUALITY_BASE=2acbf488be0e26347d20de69684ee44abe37b365 PLAYWRIGHT_CHANNEL=chrome pnpm dlx pnpm@10.30.3 verify:full
```

命令退出码：**0**。

- TypeScript：通过，包含 Next.js 路由 validator。
- 静态质量门槛：通过，无新增诊断；88 条未修改模板诊断匹配遗留基线。
- 变更格式与测试变更策略：通过。
- Vitest：**9 个测试文件、40 个测试通过**。
- 初始 6 个受保护模块：语句/行/函数 **100%**，分支 **97.77%**；满足逐文件门槛。
- 生产构建：通过。
- Playwright：**4 个 E2E 通过**，包含英文首页到定价页导航、中文首页、访客保护页重定向、匿名用户接口。
- 额外验证：ESLint 实际拒绝 `.only`、`.skip`、`.todo`；工作流 YAML 可解析；`git diff --check` 通过；冻结锁文件安装通过。

## 范围与限制

- 这是本地按 CI 环境变量执行的结果，未在远端 Actions 或 Linux Docker 上运行。
- 本地 E2E 使用已有 Google Chrome；CI 配置安装 Playwright 对应 Chromium。本地 Chromium 首次下载较慢，已停止该下载并使用文档中的 Chrome 选项完成验证。
- 43 个错误、45 个警告属于尚未修复的上游静态诊断。源码一旦修改，其遗留豁免即失效；详见 `.quality/eslint-baseline.json` 与测试指南。
- 尚未覆盖完整登录、真实数据库、支付扣费、邮件/存储或真实 AI 调用。接口集成测试替换外部依赖，E2E 使用测试配置和访客路径。
- 覆盖率只描述显式列出的 6 个模块，不能当作全项目覆盖率。
- 工作流文件尚需随代码提交；远端分支保护未修改。人工审查仍须确认测试与需求相关，CI 无法证明测试编写的时间顺序。
